/**
 * Simple Template Component.
 * Removed transitions to prioritize performance and Core Web Vitals.
 */

export default function Template({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
